import { handlers } from "@/auth";  // Import the handlers object from the auth.js file, which contains the NextAuth configuration and methods for handling authentication requests.

export const { GET, POST } = handlers;